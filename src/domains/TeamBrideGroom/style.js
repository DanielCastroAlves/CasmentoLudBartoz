import { Typography, styled } from "@mui/material";

export const TextEmBreve = styled(Typography)(({ theme }) => ({
  textAlign: "center",
  fontStyle: "italic", // Aplica estilo em itálico
  margin: 60,
  fontFamily: "cinzel",
  fontSize: "24px",
  fontWeight: 400,
  lineHeight: "29px",
  letterSpacing: "0em",
  color: "#9B4522",
}));
